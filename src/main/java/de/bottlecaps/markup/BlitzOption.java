package de.bottlecaps.markup;

public enum BlitzOption {
  /** Print information on intermediate results. */
  VERBOSE,
  /** Print timing information. */
  TIMING,
  /** Generate XML with indentation. */
  INDENT,
  /** Print parser trace. */
  TRACE,
  ;
}